# Mekong Farm Configuration
# API Keys should be set via environment variables or Settings UI

# Leave empty and set via Settings UI
gemini.api.key=

# Low stock threshold
stock.warning.threshold=10

# Auto-save interval (seconds)
auto.save.interval=30

# Session timeout (minutes)
session.timeout=60

# App settings
app.name=Mekong Farm
app.version=1.0.0
